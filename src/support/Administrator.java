package support;

public class Administrator extends User {

	public Administrator(String firstName, String lastName, String username, String password, int userType,
			int userId) {
		super(firstName, lastName, username, password, userType, userId);
	}

	
}
